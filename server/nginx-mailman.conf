server {

    listen 80;
    server_name mail.sugarcub.org;

    client_max_body_size 4G;

    access_log /shared/logs/nginx-mailman-access.log;
    error_log /shared/logs/nginx-mailman-error.log;

    location /static/ {
        alias   /shared/mailman/var/mailman-web/static/;
    }

    location /media/ {
        alias   /shared/mailman/var/mailman-web/media/;
    }

    location / {
        # an HTTP header important enough to have its own Wikipedia entry:
        #   http://en.wikipedia.org/wiki/X-Forwarded-For
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # enable this if and only if you use HTTPS, this helps Rack
        # set the proper protocol for doing redirects:
        # proxy_set_header X-Forwarded-Proto https;

        # pass the Host: header from the client right along so redirects
        # can be set properly within the Rack application
        proxy_set_header Host $http_host;

        # we don't want nginx trying to do something clever with
        # redirects, we set the Host: header above already.
        proxy_redirect off;

        # set "proxy_buffering off" *only* for Rainbows! when doing
        # Comet/long-poll stuff.  It's also safe to set if you're
        # using only serving fast clients with Unicorn + nginx.
        # Otherwise you _want_ nginx to buffer responses to slow
        # clients, really.
        # proxy_buffering off;

        # Try to serve static files from nginx, no point in making an
        # *application* server like Unicorn/Rainbows! serve static files.
		include uwsgi_params;
		uwsgi_pass unix:///shared/mailman/uwsgi.sock;
    }

    ## Error pages
    #error_page 500 502 503 504 /500.html;
    #location = /500.html {
    #    root /shared/INSTANCE/current/static/;
    #}
}

server {

    listen 443 ssl;
    server_name mail.sugarcub.org;


    ssl_certificate      ssl/server.crt;
    ssl_certificate_key  ssl/server.key;

    ssl_session_cache    shared:SSL:1m;
    ssl_session_timeout  5m;

    ssl_ciphers  HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers  on;


    client_max_body_size 4G;

    access_log /shared/logs/nginx-mailman-access.log;
    error_log /shared/logs/nginx-mailman-error.log;

    location /static/ {
        alias   /shared/mailman/var/mailman-web/static/;
    }

    location /media/ {
        alias   /shared/mailman/var/mailman-web/media/;
    }

    location / {
        # an HTTP header important enough to have its own Wikipedia entry:
        #   http://en.wikipedia.org/wiki/X-Forwarded-For
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # enable this if and only if you use HTTPS, this helps Rack
        # set the proper protocol for doing redirects:
        # proxy_set_header X-Forwarded-Proto https;

        # pass the Host: header from the client right along so redirects
        # can be set properly within the Rack application
        proxy_set_header Host $http_host;

        # we don't want nginx trying to do something clever with
        # redirects, we set the Host: header above already.
        proxy_redirect off;

        # set "proxy_buffering off" *only* for Rainbows! when doing
        # Comet/long-poll stuff.  It's also safe to set if you're
        # using only serving fast clients with Unicorn + nginx.
        # Otherwise you _want_ nginx to buffer responses to slow
        # clients, really.
        # proxy_buffering off;

        # Try to serve static files from nginx, no point in making an
        # *application* server like Unicorn/Rainbows! serve static files.
		include uwsgi_params;
		uwsgi_pass unix:///shared/mailman/uwsgi.sock;
    }

    ## Error pages
    #error_page 500 502 503 504 /500.html;
    #location = /500.html {
    #    root /shared/INSTANCE/current/static/;
    #}
}

